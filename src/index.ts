import { attribute } from "./attribute.decorator";
import { customElement } from "./custom-element.decorator";
import { 
    IAttributeChanged, IConnected, IDisconnected,
    ExternalStyle, ConditionalStyle,
} from "./interfaces";

export { 
    attribute, 
    customElement, 
    IConnected, 
    IAttributeChanged, 
    IDisconnected,
    ExternalStyle,
    ConditionalStyle,
};